// stallions.js — array of stallion profiles
// Each stallion: { id, name, sire, dam, year, handicap, offspring[], images[], pedigree }
// Will be populated in a later prompt

export const stallions = [];
